# FS-120-HDS-010-SDS-010-SMS-010 NixOS Module

Inputs: evaluated instance options and consumer image derivations. Outputs:
system closure dependencies, activation registration commands, one foreground
service per VM, generated immutable configuration, and operator packages.
Assertions implement every FS-070 contradiction and mandatory health input.
