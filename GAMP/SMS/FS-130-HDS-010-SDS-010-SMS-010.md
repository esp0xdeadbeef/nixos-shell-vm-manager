# FS-130-HDS-010-SDS-010-SMS-010 Operator Dispatchers

`vm-update`, `vm-rollout`, and `vm-status` validate a restricted VM name and map
it to `/etc/nixos-shell-vm-manager/instances/<vm>.conf`. Mutating actions require
UID zero. No command accepts a remote repository reference.
