# FS-040-HDS-010-SDS-010-SMS-010 State and Registry Commands

Implements initialization, validated admission, atomic JSON updates, GC-root
retention/pruning, status output, and slot reads. All mutation functions require
the lifecycle lock and leave the former state intact on validation failure.
