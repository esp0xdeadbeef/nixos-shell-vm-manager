# FS-120-HDS-010-SDS-010 NixOS Module Interface

The public option namespace is `services.nixosShellVmManager`. Each instance
requires `image` and `healthCheck.command`, and exposes activation, storage,
health bounds, runner arguments, and local flake attribute. Generated root-owned
configuration contains only resolved values and store paths. The module exports
no consumer repository assumptions.
