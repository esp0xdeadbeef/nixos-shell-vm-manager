# FS-010-HDS-010-SDS-010 Declarative Baseline Registrar

The NixOS module adds every instance image to `system.extraDependencies` and
generates an activation call containing the immutable output and source kind
`host-generation`. The registrar validates the runner, retains the output, and
replaces only candidate state. Repeated registration is idempotent.
