# FS-100-HDS-010-SDS-010 Runtime/Persistent Storage Adapter

The runner receives a root QCOW path below `runtimeDirectory`. When ephemeral
root is enabled it is removed only immediately before a new runner launch.
Optional state QCOW creation occurs below `persistentDirectory`; no cleanup,
promotion, or rollback operation removes that directory or its content.
