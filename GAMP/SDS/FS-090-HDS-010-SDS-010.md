# FS-090-HDS-010-SDS-010 Promotion and Rollback Transaction

The lifecycle lock covers candidate selection through health and state commit.
Promotion sets `previous=old current`, `current=candidate`, and clears candidate.
Failure sets `failed=candidate`, clears candidate, leaves current unchanged,
and verifies current after restart. No automatic path selects `failed`.
