# FS-080-HDS-010-SDS-010 Bounded Health Engine

For each configured attempt the engine first verifies the selected child PID,
then invokes the non-empty VM-specific command through `timeout`. It retries at
the configured interval and fails on child exit, timeout, or exhaustion. The
same function is called for candidate and recovery images.
