# FS-040-HDS-010 Candidate Registry and Admission Boundary

An on-host registry retains `current`, `candidate`, `previous`, and `failed`
image identities plus provenance. Admission validates the named executable and
adds an immutable GC root before atomically changing candidate metadata. It
does not control a running process.
