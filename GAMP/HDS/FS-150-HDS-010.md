# FS-150-HDS-010 Host-local Interactive Terminal

Each managed runner is connected to a per-VM terminal multiplexer endpoint on
the host. The default socket is `/run/nixos-shell/<vm>.tmux` and the default
session is `vm`. Both are per-VM inputs. The endpoint is transient, local to the
host, and independent of guest network and Internet availability.
