# FS-010-HDS-010 Host-generation Image Closure

Every enabled instance is allocated one consumer-supplied immutable image
derivation. All assigned derivations are dependencies of the NixOS system
generation. Host activation registers complete outputs but performs no start,
stop, or rollout action. A failed required image therefore fails construction
of the host generation before activation is possible.
