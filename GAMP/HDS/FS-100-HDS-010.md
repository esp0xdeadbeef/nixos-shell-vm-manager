# FS-100-HDS-010 Independent Storage Classes

Replaceable root/runtime files and persistent guest files have separate
configurable base directories. Disposable-root handling never removes the
persistent directory. Promotion and image rollback only change registry
metadata and explicitly do not rewind guest data.
