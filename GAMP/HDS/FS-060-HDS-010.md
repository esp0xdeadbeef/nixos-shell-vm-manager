# FS-060-HDS-010 Provenance and Host-activation Precedence

Candidate metadata records source kind, immutable source identity, output path,
and admission time. Each successful host activation is an explicit baseline
admission event and replaces a pending local candidate. It never changes the
known-good image. A later local admission replaces the pending baseline.
