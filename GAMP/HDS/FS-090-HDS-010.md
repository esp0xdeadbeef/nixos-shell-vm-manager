# FS-090-HDS-010 Retained Recovery Artifact and Visible Failure

The known-good GC root remains retained during candidate activation. Candidate
failure moves its identity to `failed`, starts the retained current image, and
health-checks it. Recovery failure records `operator-intervention` and makes the
public VM service fail.
