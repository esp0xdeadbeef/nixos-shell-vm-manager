# FS-050-HDS-010 Independent Construction and Runtime Resources

Construction processes and the foreground VM supervisor use separate process
and storage resources. Builds do not acquire or signal the running VM. Only a
successfully built output reaches the admission lock; failed construction
leaves every registry slot unchanged.
