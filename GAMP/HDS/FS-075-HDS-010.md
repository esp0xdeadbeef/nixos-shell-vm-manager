# FS-075-HDS-010 Host-session Start-authority State

Volatile per-VM authority state records a monotonic intent epoch and an explicit
stop marker. Service stop advances the epoch before terminating the supervisor.
Every start transition checks the current marker while holding the lifecycle
lock. Candidate admission never edits authority state.
