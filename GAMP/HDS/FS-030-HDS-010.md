# FS-030-HDS-010 Immutable Local-source Capture Environment

The root operator environment accepts an explicit local flake path, requires
its `flake.lock`, archives the source through Nix without lock mutation, and
builds only from the resulting store path. Network use is permitted during this
operator-requested construction, not during later runtime startup.
